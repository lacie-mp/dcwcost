# Netlify build configuration for the DCW Cost Management Astro site.
[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "22"

# Return the styled 404 page for unmatched routes.
[[redirects]]
  from = "/*"
  to = "/404.html"
  status = 404
